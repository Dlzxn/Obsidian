Контрольная работа (ФНП)  
(для ФММ, срок 2.06.25, сдавать на портал)

**1.** Найти $dz$ и $d^2z$ от дважды дифференцируемой функции $z = z(x, y)$, заданной неявно уравнением  
$$
F(x,\ x + y,\ x + y + z) = 0
$$

**2.** Преобразовать уравнение  
$$
\sin^2\left({x}\right) \cdot \frac{\partial^2 z}{\partial x^2}
- zy \sin x \cdot \frac{\partial^2 z}{\partial x \partial y}
+ y^2 \cdot \frac{\partial^2 z}{\partial y^2}
= 0
$$  
при помощи замены переменных  
$$
\begin{cases}
u = y \cdot \tan \frac{x}{2} \\
v = y
\end{cases}
$$

**3.** Написать уравнение касательной плоскости $\alpha$ и нормальной прямой $a$ к поверхности, заданной параметрически:  
$$
\begin{cases}
x = u + ln(v) \\
y = v - ln(u) \\
z = 2u + п
\end{cases}
$$  
в точке $M_0(1,\ 1,\ 3)$

**4.** Разложить в ряд Маклорена в окрестности точки $(0,\ 0)$ функцию  
$$
f(x, y) = \sin x \cdot \cos y
$$  
до $O(x^5)$

**5.** Исследовать на экстремум функцию  
$$
z = x^2 + xy + y^2 + \frac{1}{x} + \frac{1}{y}
$$


функция нескольких переменных

### Типы задач к зачёту

**32.** Вычислить площадь плоской фигуры, ограниченной линиями  
$y = x \arctan{x}$, $y = 0$, $0 \leq x \leq \sqrt{3}$

**33.** Вычислить площадь плоской фигуры, ограниченной линиями  
$r = 2 \sin{3\varphi}$

**34.** Вычислить объём тела, образованного при вращении данными линиями:  
$y = \frac{1}{\sqrt{x}}$, $y = 0$, $x = \frac{1}{4}$, $x = 1$

---

### Функция нескольких переменных

**35.** Найти область определения и область значений функции  
$u = \ln(x^2 + y^2 - 4)$

**36.** Найти пределы:  
$\lim\limits_{x \to 0} \lim\limits_{y \to 0} u(x, y)$  
$\lim\limits_{y \to 0} \lim\limits_{x \to 0} u(x, y)$  
$\lim\limits_{(x, y) \to (0, 0)} u(x, y)$  
где $u(x, y) = \dfrac{xy}{x^2 + y^4}$

**37.** Исследовать на дифференцируемость функцию $f$ в точке $(0, 0)$, если  
$f(x, y) = \sqrt[5]{x^4}(cos(\sqrt[5]{y})-1)$

---

**38.** Найти:  
а) дифференциал функции $f = 2\frac{y}{x} + \frac{x}{y^2}$  
б) вычислить его в точке $(1, 2)$

**39.** Найти дифференциал $\varphi = f(u, v)$, если  
$u = \dfrac{y}{x+y}$, $v = {x^2 - y^3}$

**40.** Найти производную функции $f = 5x + 10x^2y + y^5$  
в точке $M_0(1, 2)$ по направлению вектора $\vec{l} = (-3; 4)$

**41.** Показать, что функция $z = x \cdot f\left( \dfrac{y}{x^2} \right)$  
удовлетворяет уравнению:  
$x \dfrac{\partial z}{\partial x} + 2y \dfrac{\partial z}{\partial y} = z$

**42.** Проверить равенство:  
$$(x^2-y^2)\frac{\partial z}{\partial x} + xy\frac{\partial z}{\partial y} = xyz$$
Если $z=e^y\phi(ye^{x^{2}/2y^2})$
где $\varphi$ — произвольно дифференцируемая функция

**43.** Найти ${\partial^2 u}$, если  
$u = f(x^2 +y^2, z^3)$, $x$ и $y$ и $z$ — независимые переменные

**44.** Принимая $u=2x-z^2$ $v=y/z$ за новые независимые переменные преобразовать уравнение $$x\frac{\partial z}{\partial x} + y\frac{\partial z}{\partial y} = \frac{x}{z}$$.
**45.** Показать, что уравнение  
$\frac{\partial^2 z}{\partial x^2} - 2 \frac{\partial^2 z}{\partial x \partial y} + \frac{\partial^2 z}{\partial y^2}= 0$

не меняет своего вида при замене переменных:  
$u = x + z$, $v = y+z$

**46.** Преобразовать в полярные координаты:  
$x = r \cos{\varphi}$,  
$y = r \sin{\varphi}$  
выражение $W=x\frac{\partial u}{\partial y} - y\frac{\partial u}{\partial x}$

**47.** Исследовать на экстремум функцию:  
$z = 6x^2 - 7xy + 2y^2 - 6x - 3y$
